{
  "$schema": "http://json-schema.org/draft-07/schema",
  "type": "object",
  "properties": {
    "modelName": {
      "type": "string",
      "description": "Name of the model which will be set within the PMML file. Should match one of the name fields within the config.json file"
    },
    "genderSpecific": {
      "type": "boolean",
      "description": "Whether this model is stratified by gender. True if it is"
    },
    "regressionType": {
      "type": "string",
      "enum": ["CoxRegression"]
    },
    "timeMetric": {
      "type": "string",
      "enum": ["years"]
    },
    "maximumTime": {
      "type": "number",
      "description": "Maximum time expressed in terms of the timeMetric field that this model is valid until"
    }
  },
  "required": [
    "modelName",
    "genderSpecific",
    "regressionType",
    "timeMetric",
    "maximumTime"
  ]
}
